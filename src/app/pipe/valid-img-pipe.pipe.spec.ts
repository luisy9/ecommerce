import { ValidImgPipePipe } from './valid-img-pipe.pipe';

describe('ValidImgPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ValidImgPipePipe();
    expect(pipe).toBeTruthy();
  });
});
